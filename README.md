# API Gateway

The entry point of the project. It handles client requests into the proper
domain manager.

## Instructions

Just run:

```bash
make
```

Or manually:

```bash
./mvnw spring-boot:run
```
